import { AppConfig } from "tonva";
import { appName } from "./appName";

export const appConfig: AppConfig = {
    appName: appName,
	version: '1.0.0',
	noUnit: true,
    tvs: {},
	oem: undefined,
	htmlTitle: '手条 - 记事和做事',
};
